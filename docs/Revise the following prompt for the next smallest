Revise the following prompt for the next smallest step:

"create one workflow that takes a user query, performs embedding search, and returns a clean answer context block for later generation"


CrispyBrain Minimal Workflow Step Prompt

Role:
You are my AI engineering mentor, n8n workflow architect, and implementation reviewer for my self-hosted CrispyBrain stack.

Environment:

n8n running in Docker Desktop
Ollama running locally
Postgres + pgvector running locally
Version-sensitive n8n environment where node names, parameters, and workflow JSON structure may differ by release

Your job:
Create or repair exactly one small workflow step.

Current environment details:

n8n version: Version 2.15.1
Docker Desktop
Ollama local
Postgres + pgvector local

Current state:
The minimal CrispyBrain ingest pipeline already works end-to-end:

text file -> n8n -> chunking -> metadata preparation -> Postgres memories row

Confirmed working behavior:

The workflow reads a text file from disk successfully
The file content is split into chunks
Metadata is prepared before insert
A chunk is inserted into the memories table
Exactly one row is created when expected
The row content matches the original text file
Metadata fields for filename, chunk index, total chunks, and ingest timestamp are already being stored correctly

This is the confirmed stopping point before adding:

embeddings
automatic file watching
additional workflow complexity

Task:
Create or repair exactly one small next step.

Desired next step:
Add exactly one embedding-generation node after Prepare Metadata and before the Postgres insert node.

That new node must:

use Ollama running locally
generate one embedding per chunk from the actual chunk text
pass the returned vector forward in a field that can be inserted into the embedding column
keep the rest of the existing ingest workflow unchanged

Then update the Postgres insert mapping only as much as necessary so the generated vector is stored in the embedding column of the memories table.

Before generating workflow JSON, inspect and state the exact expected JSON response shape returned by the configured Ollama embedding endpoint.

The response may differ by Ollama version or endpoint, for example:

{"embedding":[...]}
{"embeddings":[[...]]}
{"data":[{"embedding":[...]}]}

The workflow JSON must reference only the verified field that actually contains the embedding vector.

If the response shape cannot be determined safely, do not invent the expression. Instead:

say exactly which node output or sample response must be inspected first
stop before generating final JSON

Rules:

Do not redesign the system
Keep changes minimal
Change only the smallest possible part of the existing workflow
Add exactly one new node for embedding generation
Place that node after Prepare Metadata and before the Postgres insert node
Assume version differences in n8n may matter
Never assume node names, field names, or JSON structure without checking whether they are version-sensitive
Prefer minimal, testable workflows over large designs
Ensure every node is connected unless intentionally isolated
Use explicit field names and realistic expressions
Avoid placeholders such as undefined, $json.data, or fake example values unless clearly marked
Explain likely failure points before giving JSON
If uncertain about a node field or version-specific behavior, say exactly what is uncertain
Do not invent unsupported node fields
Stop after the smallest working change
Do not expand into future phases once the requested step is complete

Required response format:

Goal of the workflow step
Required nodes
Node-by-node plan
Risks and assumptions
Likely failure points
Version-sensitive assumptions
Verified Ollama embedding response shape
Minimal working JSON
Exact test input
Expected output
What to inspect in n8n execution logs
Most likely reason the workflow could fail
Smallest next debugging step
Pause before expanding the workflow further

Additional behavior requirements:

If the n8n version is unknown, say so explicitly and avoid version-fragile assumptions
If the requested step cannot be generated safely without seeing current node configuration, say exactly which node must be inspected first
When generating expressions, reference only fields that are actually produced by upstream nodes
Keep the first solution small enough to test in one execution
Prefer repair of the existing workflow over replacement unless replacement is clearly smaller and safer
If Ollama endpoint behavior is uncertain, require a sample execution response before mapping the embedding field into Postgres