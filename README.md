# openbrain
Self-hosted AI memory and retrieval system built with n8n, Ollama, Postgres, and pgvector. OpenBrain incrementally ingests notes, creates embeddings, retrieves context, and generates grounded answers.
